[package]
name = "zstd"
version = "1.5.7"
type = "lib"

[language]
c = "c17"

[dependencies]
upstream = { url = "https://github.com/facebook/zstd/archive/refs/tags/v1.5.7.tar.gz" }

# Single-threaded (no ZSTD_MULTITHREAD); legacy formats and dictBuilder are
# not part of the port.
